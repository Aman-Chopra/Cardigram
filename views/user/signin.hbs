<link rel='stylesheet' href='/stylesheets/signin.css' />
<div class="row">
    <div class="col-md-4 col-md-offset-4">
      <div class="text-center login-title typewriter">
    <h1>Sign in to continue to Cardigram.</h1>
  </div>
            <div class="account-wall">
                <img class="profile-img" src="https://cdn4.iconfinder.com/data/icons/gray-toolbar-7/512/user-512.png"
                    alt="">
        {{#if hasErrors}}
            <div class="alert alert-danger">
                {{# each messages }}
                    <p>{{this}}</p>
                {{/each}}
            </div>
        {{/if}}<!--Validation, in handlebars, we can use if to check equality-->
        <form action="/user/signin" class="form-signin" method="post">
            <div class="form-group">
                <label for="email">E-Mail</label>
                <input type="text" id="email" name="email" class="form-control">
            </div>
            <div class="form-group">
                <label for="password">Password</label>
                <input type="password" id="password" name="password" class="form-control">
            </div>
            <input type="hidden" name="_csrf" value="{{ csrfToken }}"><!--Identifies if the browser sending the token
            is the same browser that received the session in the first place, which protects us from session getting stolen,
            if I remove csrf token, I will get an error-->
            <button class="btn btn-lg btn-primary btn-block" type="submit">
                    Sign in</button>

            <label class="checkbox pull-left">
                    <input type="checkbox" value="remember-me">
                    Remember me
                </label>
                <a href="/#contact" class="pull-right need-help">Need help? </a><span class="clearfix"></span>
        </form>
      </div>
       <a href="/user/signup" class="text-center new-account">Create an account </a><br>

    </div>

</div>
